# Version Notes

## v13.1.0 -> v14.0.4

分析范围：从 `v13.1.0` 到最新发布标签 `v14.0.4`。

总体变化：

- 共 6 个提交。
- 涉及 14 个文件。
- 净变化约 512 行新增、93 行删除。
- 主要改动集中在鉴权、注册流程、Team 团队账号库、Image2API 本地保存开关、库存统计与前端交互。
- 多平台 `utils/auth_core` 二进制核心文件均有更新。

### 主要变更

#### 1. 版本升级与鉴权机制

- 版本号从 `v13.1.0` 升级到 `v14.0.4`。
- 更新了 Windows、Linux、macOS 对应的 `auth_core` 二进制文件。
- 增加或调整鉴权相关逻辑。
- 优化多线程场景下鉴权容易快速封禁的问题。
- README 更新 Telegram 群组信息，并说明鉴权系统与官方群组绑定。

#### 2. 注册流程调整

- `utils/auth_pipeline/register.py` 是本轮变化最大的文件之一。
- 注册后会统一提取 `image2api` 数据。
- 常规模式新增配置项：

```yaml
normal_mode:
  save_img_to_local: false
```

- 该开关用于控制常规模式下是否把提取到的 Image2API 数据保存到本地账号库。
- 修复常规模式打开 Img 开关后注册失败的问题。
- 优化授权链路、工作区选择逻辑、注册停止时的异常处理。
- 增加 Team 模式相关的静默流程，注册过程中可调用系统节点分配与释放。

#### 3. 新增 Team 团队账号库

- 新增配置项：

```yaml
team_mode:
  enable: false
```

- 数据库新增 `team_accounts` 表。
- 新增 Team Token 导入、分页查询、删除、清空、随机取用等数据库方法。
- 新增后端 API：
  - `GET /api/team_accounts`
  - `POST /api/team_accounts/import`
  - `POST /api/team_accounts/delete`
  - `POST /api/team_accounts/clear_all`
- 前端新增 “Team 账号库” 页面，支持：
  - 批量导入 Team Token。
  - 明文/脱敏显示。
  - 单条删除。
  - 一键清空。
  - Team 模式开关。

#### 4. 本地账号库与推送体验优化

- 本地账号库中，如果选中的账号是 `image2api` 类型，则不再显示 CPA / Sub2API 推送按钮，避免误推。
- 继续增强 CPA、Sub2API、Image2API 三个平台的同步展示。
- 当账号同时存在三个平台标识时，前端显示统一的“三平台同步”标志。

#### 5. MySQL 与库存统计相关修复

- 有提交专门修复 “MySQL 出现库存异常问题”。
- 库存统计逻辑围绕本地库存、云端库存、CPA、Sub2API 等维度继续调整。
- 注意：`v14.0.4` 中 `utils/db_manager.py` 的库存统计 SQL 存在一个疑点，代码里出现了字面量 `'{p}CPA{p}'` 和 `'{p}SUB2API{p}'`，可能导致 CPA/Sub2API 统计不符合预期，建议后续单独确认。

#### 6. 前端与部署细节

- `wfxl_openai_regst.py` 将静态文件挂载从相对路径改为基于程序目录的绝对路径，减少不同启动目录下找不到 `static` 的问题。
- `index.html` 和 `static/js/app.js` 增加 Team 页面、配置默认值、账号库操作逻辑。
- `.gitignore` 增加测试文件忽略项。

### 提交脉络

- `554fe25`：修复 MySQL 出现库存异常问题。
- `8512866` / `v14.0.0`：新增鉴权机制、常规模式 Img 保存开关、Team 账号库和功能开关，优化本地账号库推送按钮显示。
- `16f425f` / `v14.0.1`：优化多线程鉴权秒封问题，优化注册停止时异常问题，更新群组信息。
- `f3ef01d` / `v14.0.2`：修复导入问题。
- `c0437fd` / `v14.0.3`：修复授权和注册相关问题。
- `b72fbf0` / `v14.0.4`：修复常规模式打开 Img 开关注册失败问题，更新群组信息。

### 备注

本总结基于发布标签 `v13.1.0..v14.0.4`。当前本地工作区存在未提交/冲突/本地改动，这些未纳入本次版本总结。
