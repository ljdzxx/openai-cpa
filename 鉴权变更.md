• 结论已经比较清楚：v14.0.0 是“新增鉴权机制”的主提交，v14.0.1 和 v14.0.3 是后续调整核心，v14.0.2 是 Team Token 导入解析修复，v14.0.4 只碰到注册链路的一个副作用修复。

• 从 Git 记录看，“增加或调整鉴权相关逻辑”不是一句泛泛描述，实际分布在这几个版本：

  v14.0.0 / 8512866：主版本，真正“新增鉴权机制”

  这是最核心的一版，commit message 明确写了“增加鉴权机制”。

  涉及文件：

  - utils/auth_core.cpython-311-aarch64-linux-gnu.so
  - utils/auth_core.cpython-311-darwin.so
  - utils/auth_core.cpython-311-x86_64-linux-gnu.so
  - utils/auth_pipeline/register.py
  - routers/account_routes.py
  - utils/config.py
  - config.example.yaml
  - utils/db_manager.py
  - index.html
  - static/js/app.js

  能从源码确认的变化：

  - register.py 新增从 utils.auth_core 导入：
      - sys_node_allocate
      - sys_node_release
  - 注册流程里新增 Team 静默流程：
      - 提取 image2api_data
      - 如果开启 TEAM_MODE_ENABLE，调用 sys_node_allocate
      - 授权流程成功、失败、退出时调用 sys_node_release
  - account_routes.py 新增从 utils.auth_core 导入：
      - email_jwt
  - Team Token 导入时用 email_jwt 解析 token 里的邮箱。
  - config.py / config.example.yaml 新增：
      - team_mode.enable
      - TEAM_MODE_ENABLE
  - 前端增加 Team 账号库和 Team 模式开关。

  这里的 auth_core 是二进制文件，不能直接看内部 diff，但从新增导入函数和调用点看，这一版应该是把 Team 静默鉴权、Token 解析、节点分配释放等能力放进了核心模块。

  v14.0.1 / 16f425f：优化多线程下鉴权秒封问题

  commit message 明确写了“优化多线程下鉴权秒封问题”。

  涉及文件：

  - utils/auth_core.cpython-311-aarch64-linux-gnu.so
  - utils/auth_core.cpython-311-darwin.so
  - utils/auth_core.cpython-311-x86_64-linux-gnu.so
  - utils/auth_core.pyd
  - utils/auth_pipeline/register.py
  - utils/config.py
  - README.md

  源码层面主要是 register.py 里对 sys_node_release(...) 做了更宽松的保护：

  - 原来释放前判断 saved_temp_at and sys_handle_a and sys_handle_b
  - 后来改成直接 try/except 包裹释放调用

  这更像是为多线程/异常路径做容错，避免释放逻辑本身导致注册流程异常。核心鉴权细节大概率在 auth_core 二进制里。

  v14.0.2 / f3ef01d：修复导入问题，关联 Team Token 解析

  不算核心鉴权新增，但和 email_jwt 解析有关。

  涉及文件：

  - routers/account_routes.py
  - utils/db_manager.py
  - utils/config.py

  关键变化：

  - email_jwt(acc_token) 的返回值从直接当邮箱使用，改成按 dict 取：
      - jwt_data = email_jwt(acc_token)
      - real_email = jwt_data.get("email", "")
  - 说明 email_jwt 实际返回的是 JWT 解析后的结构，而不是字符串邮箱。
  - 数据库补了 team_accounts.access_token 字段兼容逻辑。

  v14.0.3 / c0437fd：修复授权和注册一系列问题

  这是第二个直接涉及授权链路的版本。

  涉及文件：

  - utils/auth_core.cpython-311-aarch64-linux-gnu.so
  - utils/auth_core.cpython-311-darwin.so
  - utils/auth_core.cpython-311-x86_64-linux-gnu.so
  - utils/auth_core.pyd
  - utils/auth_pipeline/register.py
  - utils/config.py

  源码层面的变化集中在 register.py：

  - 调整 Image2API 数据保存逻辑。
  - 把 IMAGE2API_RETAIN_REG_ONLY 的本地保存逻辑从“远端同步成功后才保存”改成更独立的保存路径。
  - Team 静默流程仍依赖 image2api_data 产生的 access token，再走 sys_node_allocate。

  二进制 auth_core 同时更新，结合 commit message，可以归类为“授权/注册链路修复”。

  v14.0.4 / b72fbf0：常规模式 Img 开关副作用修复

  涉及文件：

  - utils/auth_pipeline/register.py
  - utils/config.py
  - README.md

  关键变化很小：

  - 删除了常规模式保存 Image2API 时重复调用 image2api_data(...) 的一行。
  - 这解决“常规模式打开 Img 开关注册失败”的问题。
  - 不属于新增鉴权机制，但影响注册后提取凭据这条链路。

  补充

  554fe25 也改过 auth_core 二进制文件，但提交说明是“修复 mysql 出现库存异常问题”，没有明确指向鉴权；我不会把它归为“鉴权机制新增”，只能说它也携带了核心二进制更新。
  
  # 授权改开源
  C:\Users\ray\Documents\JuCodex\code\openai-cpa\utils\auth_pipeline\register.py:517
  C:\Users\ray\Documents\JuCodex\code\openai-cpa\routers\account_routes.py